import React, { useEffect, useState } from "react";

export default function Logistica() {
  useEffect(() => {
    window.scrollTo(0, 0);
  }, []);
  return (
    <div>
      <p>Nome do curso</p>
    </div>
  );
}
